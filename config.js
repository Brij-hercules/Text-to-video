window.APP_CONFIG = window.APP_CONFIG || {
  // For local testing keep localhost; for Vercel set your deployed backend URL.
  // Example: "https://your-backend.onrender.com"
  BACKEND_URL: "http://localhost:8000"
};
